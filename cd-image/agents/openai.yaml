interface:
  display_name: "CD-image"
  short_description: "Route image requests to Gemini or Image2 by key."
  brand_color: "#00A6A6"
  default_prompt: "Use $cd-image to generate or edit an image with the best image model available to my API key."

policy:
  allow_implicit_invocation: true
